package at.qe.sepm.skeleton.model;

/**
 * Enum representing the type of a {@link Question}. Depending on the type all strings of a Question are interpreted differently.
 */
public enum QuestionType
{
	text, picture, math
}